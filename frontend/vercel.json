{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"homologacion\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"produccion\" ]; then exit 1; else exit 0; fi"
}